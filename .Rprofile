
options(repos = "https://cloud.r-project.org",
        pkgType = "both",
        useFancyQuotes = FALSE,
        warnPartialMatchAttr = TRUE,
        warnPartialMatchDollar = TRUE,
        warnPartialMatchArgs = TRUE,
        keep.source = TRUE,
        keep.source.pkgs = TRUE,
        rasterTmpTime = 1,     # reduce raster temp storage
        rasterMaxMemory = 1e9,
        max.print = 1000,
        "devtools.desc" = list(
           Author = "Noam Ross",
           Maintainer = paste0("Noam Ross", " <", "noam.ross@gmail.com", ">"),
           License = "MIT + file LICENSE",
           Version = "0.0.1"),
        "devtools.name" = "Noam Ross"
)


utils::rc.settings(ipck = TRUE)

